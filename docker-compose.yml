services:
  frontend:
    image: frontend-app
    build:
      context: .
    container_name: frontend-app
    ports:
      - "82:80"
    networks:
      - app_network
networks:
  app_network:
    external: true
